[rmk]
# Space size in bytes for storing macro sequences, default value is 256 [bytes]
macro_space_size = 2048

# Increase report channel to reduce backpressure-induced freezes (default is 8)
report_channel_size = 32

vial_channel_size = 8

[event.action]
subs = 1